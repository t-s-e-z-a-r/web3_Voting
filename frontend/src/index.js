import React from 'react';
import ReactDOM from 'react-dom';
import BlockchainData from './App'; // Вказати правильний шлях до файлу

ReactDOM.render(
  <React.StrictMode>
    <BlockchainData />
  </React.StrictMode>,
  document.getElementById('root')
);
// reportWebVitals();
